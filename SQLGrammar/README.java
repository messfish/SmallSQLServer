package SQLGrammar;

/**
 * For this package, it was designed to check whether there are any 
 * syntax errors and grammar mistakes in this package. 
 * 
 * These are the functions that this small interpreter couLd afford:
 * 
 * This is the SQL query part:
 * SELECT, FROM, WHERE, GROUP BY, AS, LIKE, TOP, LIMIT, ORDER BY
 * DISTINCT, LEFT OUTER JOIN, RIGHT OUTER JOIN, FULL JOIN.
 * UNION, INTERSECT, EXCEPT.
 * 
 * This is the SQL write part:
 * INSERT INTO - VALUES;
 * DELETE - WHERE;
 * UPDATE - WHERE;
 * COMMIT and ROLLBACK;
 * CREATE DROP ALTER
 * 
 * This is the constraint on the small SQL interpreter:
 * CHECK, FOREIGN KEY, PRIMARY KEY.
 * 
 * These are the data types that could be used for the SQL parser:
 * INT, REAL, CHAR, DATE, TIME, DEFAULT.
 * 
 * @author messfish
 *
 */

